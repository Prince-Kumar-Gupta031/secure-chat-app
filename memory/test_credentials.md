# DRDO Secure Chat — Test Credentials

## Super Admin (seeded automatically on backend startup)
- Mobile: `7209674114`
- Password: `admin`
- Role: super_admin
- Status: approved
- Note: `must_change_password=true` flag is set; UI shows banner but does not block.

## Test Employee (must be created via registration + admin approval to use)
- Register via UI → Admin approves via dashboard → Login
